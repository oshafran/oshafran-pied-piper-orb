description: >
  Sample description
# What will this job do?

executor: default

parameters:
  dir:
    type: string
    default: "/root/project/collections"
    description: "Hello to whom?"
steps:
  - attach_workspace:
      at: /root/project
  - pipeline:
      dir: << parameters.dir >>